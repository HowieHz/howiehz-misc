# Changesets

Run `pnpm changeset` after making a releasable change to `compat-finder`.

Changesets are used here to:

- collect release notes with each change
- update package versions
- generate changelog entries
- drive the npm release workflow
