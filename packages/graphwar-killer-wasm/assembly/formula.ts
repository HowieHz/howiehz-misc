import { requireArenaInitialized } from "./memory";

/** Placeholder for formula construction and canonical expression-program evaluation. */
export function runFormula(): i32 {
  requireArenaInitialized();
  return 0;
}
